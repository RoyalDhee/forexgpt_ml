
pypdf>=4.0.0
pdfplumber>=0.10.0

# OCR (for image-based / Seeking Alpha screenshot PDFs) 
pdf2image>=1.17.0
pytesseract>=0.3.10
Pillow>=10.0.0

openai>=1.0.0
python-dotenv>=1.0.0
tqdm>=4.66.0

# ForexGPT Fine-Tuning Requirements
# For fine-tuning Mistral-7B-Instruct with LoRA

# Core ML libraries
torch>=2.0.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.25.0
peft>=0.7.0
trl>=0.7.0

# Quantization
bitsandbytes>=0.41.0

# Utilities
sentencepiece
protobuf
scipy

# Optional: Monitoring
wandb  # Uncomment if you want to use Weights & Biases for tracking

# Optional: Jupyter for notebooks
# jupyter
# ipykernel
